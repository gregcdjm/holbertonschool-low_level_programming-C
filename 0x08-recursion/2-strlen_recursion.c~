#include "main.h"
#include <stdio.h>
/**
 * _strlen_recursion - g
 * @s: string
 * Return: string
 */
int _strlen_recursion(char *s)
{
	if (*s != 0)
	{
		return(	_strlen_recursion(s + 1));
	}
	return (_strlen_recursion(s + 1)));
}
