#include "main.h"

/**
 * print_array - check the code
 * @a: string
 * @n: n
 * Return: Always 0.
 */
void print_array(int *a, int n)
{
	int b = 0;

	for (n = n; n > 1; n--)
	{
		_putchar('a[b]'), _putchar(','), _putchar(' ');
		b++;
	}
	_putchar('a[b]');
	_putchar('\n');
}
