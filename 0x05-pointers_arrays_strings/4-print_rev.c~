#include "main.h"

/**
 * print_rev - check the code
 * @s: s
 * Return: Always 0.
 */
void print_rev(char *s)
{
	int a = 0;

	while (s[a] != 0)
		a++;
	while (a > 0)
	{
		_putchar(s[a]);
		a--;
	}
	_putchar('\n');
}
