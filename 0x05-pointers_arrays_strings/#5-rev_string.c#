#include "main.h"

/**
 * rev_string - check the code
 * @s: string
 * Return: Always 0.
 */
void rev_string(char *s)
{
	int a = 0, b = 0;

	while (s[a] != 0)
		a++;
	a = a - 1;
	while (s[a] != s[b])
	{
		s[a] = s[b];
		a--;
		b++;
	}
}
