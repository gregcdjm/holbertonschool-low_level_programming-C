#include "main.h"

/**
 * puts2 - check the code
 * @str: string
 * Return: Always 0.
 */
void puts2(char *str)
{
	int a = 0, b = 0;

	while (s[a] != 0)
		a++;
	while (b < a)
	{
		_putchar(str[b]);
		b++;
		b++
	}
}
