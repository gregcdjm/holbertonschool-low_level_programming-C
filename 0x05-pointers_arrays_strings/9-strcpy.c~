#include "main.h"

/**
 * *_strcpy - check the code
 * @dest: string
 * @n: n
 * Return: Always 0.
 */
char *_strcpy(int *dest, char *src)
{
	int b;

	for (b = 0; b < n; b++)
	{
		if (b >= n - 1)
			printf("%d", a[b]);
		else
			printf("%d, ", a[b]);
	}
	printf("\n");
}
