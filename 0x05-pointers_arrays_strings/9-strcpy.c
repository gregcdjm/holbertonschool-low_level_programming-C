#include "main.h"

/**
 * *_strcpy - check the code
 * @dest: string
 * @src: n
 * Return: Always 0.
 */
char *_strcpy(int *dest, char *src)
{
	while (dest[i] != 0)
	{
		dest[i] = src[i], i++;
	}
	return (dest);
}
