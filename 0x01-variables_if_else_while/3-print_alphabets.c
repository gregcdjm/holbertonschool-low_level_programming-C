#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
* main - main
* Return: return
*/
int main(void)
{
int ch;
int hc;
for (ch = 'a'; ch <= 'z'; ch++)
putchar(ch);
for (hc = 'A'; hc <= 'Z'; hc++)
putchar(hc);
puts("");
return (0);
}
