#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
* main - main
* Return: return
*/
int main(void)
{
int ch;
for (ch = 'a'; ch <= 'z'; ch++)
RemoveChar(char *ch,char c):1
putchar(ch);
puts("");
return (0);
}
