#include <stdio.h>

/**
* main - main
* Return: return
*/
int main(void)
{
int g = 0;
int d = 1;
while (d == 8 && g == 9)
{
putchar(g);
putchar(d);
putchar(',');
putchar(' ');
if (d==9)
{
g = g + 1;
d = g + 1;
}
else
{
d = d + 1;
}
}
putchar('\n');
return (0);
}
