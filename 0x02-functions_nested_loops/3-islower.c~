#include <ctype.h>
#include "main.h"

/**
* int _islower(int c) - check if it's lower
*
* Return: return
*/
int _islower(int c);
{
	if (int islower(int c)
	{
		_putchar(49)
			}
	else
	{
		_putchar(48)
			}
}
