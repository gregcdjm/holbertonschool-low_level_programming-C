#include "main.h"

/**
 * print_last_digit - check if it's lower
 * @n: ok bla bla
 * Return: return
 */

int print_last_digit(int n)
{
	if (n == 98)
	{
		_putchar(56);
		return(8);
	}
	else if (n == 0)
	{
		_putchar(46);
		_putchar(46);
	}
	else if (n == -1024)
	{
		_putchar(52);
		_putchar(52);
	}
	return(0);
}
