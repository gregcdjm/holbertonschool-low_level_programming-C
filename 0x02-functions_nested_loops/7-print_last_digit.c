#include "main.h"

/**
 * print_last_digit - check if it's lower
 * @n: ok bla bla
 * Return: return
 */

int print_last_digit(int n)
{
	int _abs(int n);
	n = n % 10;
	_putchar(n);
	_putchar(n);
	return (0);
}
