#include "main.h"

/**
 * print_last_digit - check if it's lower
 * @n: ok bla bla
 * Return: return
 */

int print_last_digit(int n)
{
	int H = 0, m;

	while (H < 24)
	{
		m = 0;
		while (m != 59)
		{
			for (m = 0; m <= 59; m++)
				H++;
		}
	}
}
