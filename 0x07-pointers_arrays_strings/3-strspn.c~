#include "main.h"

/**
 * _strspn g
 * @s: string
 * @accept: chars
 * Return: length
 */

unsigned int _strspn(char *s, char *accept)
{
	int a, b, c = 0;

	for (a = 0; accept[m] != 0; a++)
		for (b = 0; s[n] != 0 && s[n] != ','; n++)
			if (s[n] == accept[m])
				c++;
}
