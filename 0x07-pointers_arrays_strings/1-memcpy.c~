#include "main.h"

/**
 *_memset - fils the first n bytes
 * @s: pointer to s
 * @b: constant byte
 * @n: the n byte
 * Return: s memory area
 */

char *_memset(char *s, char b, unsigned int n)
{
	unsigned int i;

	for (i = 0; i < n; i++)
		dest[i] = src[i];
	return (dest);
}
