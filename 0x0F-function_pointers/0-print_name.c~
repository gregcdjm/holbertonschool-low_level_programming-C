#include <stdio.h>
#include "function_pointers.h"

/**
 * print_name - prints a name as is
 * @name: name of the person
 * @f: f
 * Return: Nothing.
 */
void print_name(char *name, void (*f)(char *))
{
	printf("Hello, my name is %s and %s\n", name, void (*)(char *));
}
