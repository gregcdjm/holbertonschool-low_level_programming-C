#include <stdio.h>
/**
 * main - prints its name
 * @argc: numbers of argu
 * @argv: argu string
 * Return: succes 0
 */
int main(int argc, char **argv)
{
	(void)*argv;
	if (argc == 1)
		printf("0/n");
	else
		printf("%d\n", argc);
	return (0);
}
