#include "main.h"

/**
 * _strcmp - cat strxsy
 * @s1: argum 1
 * @s2: argum 2
 * Return: dest
 */

int _strcmp(char *s1, char *s2)
{
	int i;
	int b = 0;

	for (i = 0; s1[i] != 0; i++)
	{
		if (s1[i] != s2[i])
		{
			b++;
		}
	}
	return (b);
}
